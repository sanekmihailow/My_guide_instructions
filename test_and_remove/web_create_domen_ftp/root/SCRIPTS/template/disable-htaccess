<IfModule mod_rewrite.c>
    OPTIONS +Followsymlinks
    RewriteEngine On
    RewriteRule (.*) https://domain.com/pay?name=servweb [R,L]
</IfModule>

#<IfModule mod_headers.c>
#        Header append Cache-Control "no-store, no-cache, must-revalidate"
#</IfModule>
